---
- hosts: localhost
  connection: local
  roles:
    - { role: ../roles/git, git_gitconfig_backup: no }
    - { role: ../roles/package_manager, tags: ['bootstrap'] }
    - { role: ../roles/zsh, tags: ['bootstrap'] }
